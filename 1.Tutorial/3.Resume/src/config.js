/**
 * Lambda 함수 URL.
 *
 * 4.lambda 실습에서 만든 함수 URL을 따옴표 안에 붙여넣는다.
 * 예: export const LAMBDA_URL = 'https://abc123.lambda-url.us-east-1.on.aws';
 *
 * 비워 두면 방문자수/좋아요 기능은 호출 없이 안내 문구만 표시된다.
 * 끝에 슬래시(/)는 넣지 않는다.
 */
export const LAMBDA_URL = '';
